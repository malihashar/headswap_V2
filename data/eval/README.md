# Eval set

Synthetic pairs are generated by `scripts/prepare_eval_set.py` for CI.

For real GPU benchmarking, replace `bodies/` and `faces/` with consented photos and keep the same filenames listed in `pairs.json` (or regenerate the manifest).
Prefer diverse: pose, lighting, hair length mismatch, occlusion, skin tone.
