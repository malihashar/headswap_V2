name: flux_kontext
pipeline: flux_kontext

# FLUX.1 Kontext [dev] FP8 — Align → Paste → Refine (community face-swap path).
unet_name: flux1-dev-kontext_fp8_scaled.safetensors
clip_name1: clip_l.safetensors
clip_name2: t5xxl_fp8_e4m3fn_scaled.safetensors
clip_type: flux
vae_name: ae.safetensors

# Placement LoRA (Put it here V4 / Place It). Auto-discovers under ComfyUI/models/loras
# when unset; set explicitly to force a filename.
placement_lora: null
placement_lora_strength: 1.0

steps: 28
cfg: 1.0
flux_guidance: 3.5
sampler: euler
scheduler: simple
seed: 46
# Refine the pasted composite (not full empty-canvas regen).
denoise: 0.85
empty_latent: false
max_dim: 768
div_by: 8

# Feature toggles (community defaults = on)
reference_latent: true
conditioning_zero_out: true
image_scale: true
# alias kept for back-compat with earlier configs
flux_kontext_image_scale: true

# Soft locality after refine
mask_expand_px: 18
mask_blur_px: 12

# Put it here / Place It trigger language — keep short and face-local.
prompt: >-
  Put it here. Natural face blend, match lighting and skin tone,
  preserve same pose, clothing, background, and body. No style change.

negative_prompt: ""

face_side_pad: 0.35
face_top_pad: 0.65
face_bot_pad: 0.15
