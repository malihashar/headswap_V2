name: krea2_full_image_synth
pipeline: krea2_full_image_synth

# Experimental: full-image Krea2 Identity Edit with ZERO post-localization.
# Does NOT modify configs/krea2_identity_edit.yaml (localized production path).
#
# Identity-path design:
#   - Landmark-based FULL HEAD crop (hair/beard/ears/neck) — not tight face box
#   - Face-filled square canvas (fit_mode handles AR vs scene)
#   - Replace head/face/hair prompt
#   - ref_boost high on image-2; ref_boost_a neutral on scene
#   - No stitch/freeze/blend after generation (cannot restore original hair)

unet_name: krea2_turbo_fp8_scaled.safetensors
clip_name: qwen3vl_4b_fp8_scaled.safetensors
clip_type: krea2
vae_name: qwen_image_vae.safetensors

identity_lora_name: krea2_identity_edit_v1_2_r64.safetensors
identity_lora_strength: 1.0

steps: 12
cfg: 1.0
sampler: euler
scheduler: simple
seed: 46
denoise: 1.0

apply_timestep_shift: true
timestep_shift_mu: 1.15
timestep_base_shift: 0.5

force_full_load: true
skip_negative_grounding: true
debug_count_unet_forwards: false
torch_compile: false
save_debug: true
verbose: true

ref_boost: 5.0
ref_boost_a: 1.0
grounding_px: 768
fit_mode: fit
max_dim: 1024
div_by: 16

# Full-head identity canvas
identity_use_head_crop: true
identity_head_fill: 0.88
identity_long_side: 768
identity_square_fill: true
identity_fill_frac: 0.90
identity_white_bg: true
use_ref_boost_mask: true

# Target face selection for multi-person scenes.
face_select_policy: largest
face_index: 0

# Legacy face-pad path only (identity_use_head_crop: false).
face_side_pad: 0.22
face_top_pad: 0.70
face_bot_pad: 0.12

use_vlm_caption: false
instruction_suffix: null
prompt_prefix: null

negative_prompt: ""
